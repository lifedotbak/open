<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spyker.commons.mapper.SysUserPostMapper">


    <select id="query" resultType="com.spyker.commons.entity.SysUserPost"
            parameterType="com.spyker.commons.search.SysUserPostSearch">
        SELECT
        a.*
        FROM
        sys_user_post a
        <where>
        </where>
    </select>

    <select id="queryPage" resultType="com.spyker.commons.entity.SysUserPost"
            parameterType="com.spyker.commons.search.SysUserPostSearch">
        SELECT
        a.*
        FROM
        sys_user_post a
        <where>
        </where>
    </select>

</mapper>